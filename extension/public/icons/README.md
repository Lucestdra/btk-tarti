# Icons

Manifest V3 PNG ikonları `scripts/build-icons.mjs` ile üretilir. Tasarım,
extension panelindeki logo ile aynı: dört sinyal noktası tek karar noktasına
bağlanır.

Yeniden üretmek için:

```bash
npm run icons:build
```
